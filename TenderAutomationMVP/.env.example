# 🚀 Tender Automation MVP - Environment Configuration
# Скопируйте этот файл в .env и настройте под ваше окружение

# =============================================================================
# 🖥️ SERVER CONFIGURATION
# =============================================================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s
SERVER_SHUTDOWN_TIMEOUT=10s

# =============================================================================
# 🗃️ DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_password
DB_NAME=tender_automation
DB_SSLMODE=disable

# Connection pool settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=1h
DB_CONN_MAX_IDLE_TIME=30m

# =============================================================================
# 🤖 AI CONFIGURATION (Ollama/Llama)
# =============================================================================
# TODO: Настроить подключение к Ollama
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama2
# Альтернативные модели:
# OLLAMA_MODEL=llama4-maviric
# OLLAMA_MODEL=codellama

# AI request settings
AI_TIMEOUT=60s
AI_MAX_RETRIES=3
AI_RETRY_DELAY=5s

# =============================================================================
# 🕷️ WEB SCRAPING CONFIGURATION
# =============================================================================
# Rate limiting для избежания блокировок
SCRAPING_DELAY=2s
SCRAPING_MAX_CONCURRENT=3
SCRAPING_USER_AGENT="Mozilla/5.0 (compatible; TenderBot/1.0)"

# Retry settings
SCRAPING_MAX_RETRIES=3
SCRAPING_RETRY_DELAY=5s

# Timeouts
SCRAPING_REQUEST_TIMEOUT=30s
SCRAPING_PAGE_TIMEOUT=60s

# =============================================================================
# 📝 LOGGING CONFIGURATION
# =============================================================================
LOG_LEVEL=info
# Возможные уровни: debug, info, warn, error, fatal
LOG_FORMAT=json
# Возможные форматы: json, console
LOG_OUTPUT=stdout
# Возможные выходы: stdout, stderr, file

# Development settings
LOG_ENABLE_CALLER=true
LOG_ENABLE_STACKTRACE=false

# =============================================================================
# 🧪 DEVELOPMENT SETTINGS
# =============================================================================
# Режим работы: development, production, testing
APP_ENV=development
APP_DEBUG=true

# Graceful shutdown
GRACEFUL_SHUTDOWN_TIMEOUT=30s

# =============================================================================
# 📊 MONITORING (опционально)
# =============================================================================
# Metrics endpoint
METRICS_ENABLED=true
METRICS_PATH=/metrics

# Health check endpoint
HEALTH_CHECK_PATH=/health

# =============================================================================
# 🔒 SECURITY SETTINGS
# =============================================================================
# CORS settings для фронтенда (если планируется)
CORS_ALLOWED_ORIGINS="http://localhost:3000,http://localhost:8080"
CORS_ALLOWED_METHODS="GET,POST,PUT,DELETE,OPTIONS"
CORS_ALLOWED_HEADERS="*"
CORS_ALLOW_CREDENTIALS=true

# Request limits
REQUEST_TIMEOUT=30s
MAX_REQUEST_SIZE=10MB

# =============================================================================
# 🎯 BUSINESS LOGIC SETTINGS
# =============================================================================
# Настройки анализа тендеров
MIN_AI_SCORE_THRESHOLD=0.7
# Минимальная оценка AI для релевантного тендера

DEFAULT_TENDER_STATUS=active
# Статус тендера по умолчанию при создании

# Pagination defaults
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# =============================================================================
# 🔄 SCHEDULED JOBS (для будущего расширения)
# =============================================================================
# Интервалы выполнения задач
TENDER_DISCOVERY_INTERVAL=1h
AI_ANALYSIS_INTERVAL=30m
CLEANUP_INTERVAL=24h

# =============================================================================
# 📝 ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =============================================================================

# Временная зона
TZ=Europe/Moscow

# Локаль
LANG=ru_RU.UTF-8

# =============================================================================
# 🚨 ВАЖНЫЕ ПРИМЕЧАНИЯ:
# =============================================================================
# 1. НЕ КОММИТЬТЕ файл .env в репозиторий!
# 2. Используйте безопасные пароли в production
# 3. Настройте SSL для production базы данных
# 4. Регулярно обновляйте секреты
# 5. Используйте отдельные настройки для каждого окружения
#
# Для production:
# - Установите APP_ENV=production
# - Увеличьте таймауты для большой нагрузки
# - Включите SSL для базы данных (DB_SSLMODE=require)
# - Настройте мониторинг и логирование
# - Используйте переменные окружения вместо .env файла
#
# Для testing:
# - Установите APP_ENV=testing
# - Используйте отдельную тестовую БД
# - Уменьшите таймауты для быстрых тестов
# - Включите подробное логирование (LOG_LEVEL=debug)
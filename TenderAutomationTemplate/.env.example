# 🔧 КОНФИГУРАЦИЯ СИСТЕМЫ АВТОМАТИЗАЦИИ ТЕНДЕРОВ
# Скопируйте этот файл в .env и настройте под ваше окружение

# ===== ОСНОВНЫЕ НАСТРОЙКИ ПРИЛОЖЕНИЯ =====
APP_NAME="Tender Automation System"
APP_VERSION="1.0.0"
APP_ENVIRONMENT=development  # development, production
APP_PORT=8080
APP_HOST=0.0.0.0

# Секрет для JWT токенов (сгенерируйте случайную строку 32+ символов)
JWT_SECRET=your-super-secret-jwt-key-here-change-me-in-production
SESSION_TIMEOUT=24h

# ===== БАЗЫ ДАННЫХ =====

# PostgreSQL (основная БД для тендеров, поставщиков, пользователей)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DBNAME=tender_automation
POSTGRES_SSLMODE=disable
POSTGRES_MAX_OPEN_CONNS=25
POSTGRES_MAX_IDLE_CONNS=5
POSTGRES_CONN_MAX_LIFETIME=1h

# MongoDB (для документов и неструктурированных данных)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=tender_documents
MONGODB_TIMEOUT=30s

# Redis (для кеширования и очередей задач)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_POOL_SIZE=10

# ===== ИСКУССТВЕННЫЙ ИНТЕЛЛЕКТ =====

# Выбор провайдера: ollama, openai, anthropic
AI_PROVIDER=ollama

# Ollama (локальная модель Llama 4 Maviric)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama4-maviric:latest

# OpenAI (альтернатива локальной модели)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4

# Общие настройки ИИ
AI_MAX_TOKENS=4000
AI_TEMPERATURE=0.1
AI_REQUEST_TIMEOUT=5m
AI_MAX_RETRIES=3
AI_CONCURRENT_REQUESTS=5

# ===== WEB SCRAPING =====

# Основные настройки парсинга
SCRAPING_USER_AGENT="TenderBot/1.0 (Medical Equipment Scanner)"
SCRAPING_REQUEST_DELAY=2s
SCRAPING_TIMEOUT=30s
SCRAPING_MAX_CONCURRENCY=5
SCRAPING_RETRY_COUNT=3
SCRAPING_USE_HEADLESS=true
SCRAPING_PROXY_ROTATION=false

# Настройки для zakupki.gov.ru
PLATFORM_ZAKUPKI_ENABLED=true
PLATFORM_ZAKUPKI_BASE_URL=https://zakupki.gov.ru
PLATFORM_ZAKUPKI_RATE_LIMIT=5s
PLATFORM_ZAKUPKI_MAX_PAGES=10

# Настройки для szvo.gov35.ru
PLATFORM_SZVO_ENABLED=true
PLATFORM_SZVO_BASE_URL=https://szvo.gov35.ru
PLATFORM_SZVO_RATE_LIMIT=3s
PLATFORM_SZVO_MAX_PAGES=5

# Настройки для gz-spb.ru
PLATFORM_SPB_ENABLED=true
PLATFORM_SPB_BASE_URL=https://gz-spb.ru
PLATFORM_SPB_RATE_LIMIT=7s
PLATFORM_SPB_MAX_PAGES=3

# ===== EMAIL АВТОМАТИЗАЦИЯ =====

# SMTP для отправки писем поставщикам
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password  # Используйте App Password для Gmail
SMTP_USE_TLS=true

# IMAP для чтения ответов от поставщиков
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USERNAME=your-email@gmail.com
IMAP_PASSWORD=your-app-password
IMAP_USE_TLS=true

# Общие настройки email
EMAIL_FROM_EMAIL=your-email@gmail.com
EMAIL_FROM_NAME="Tender Automation System"
EMAIL_REPLY_TO=your-email@gmail.com
EMAIL_MAX_EMAILS_PER_DAY=100
EMAIL_SEND_DELAY=1m
EMAIL_AI_TEMPLATES=true

# ===== ОБРАБОТКА ДОКУМЕНТОВ =====

# Настройки обработки файлов
DOC_TEMP_DIR=/tmp/tender_docs
DOC_MAX_FILE_SIZE=52428800  # 50MB в байтах
DOC_PROCESSING_TIMEOUT=10m
DOC_PRESERVE_ORIGINAL=true
DOC_OCR_ENABLED=true
DOC_EXTRACT_TABLES=true
DOC_EXTRACT_IMAGES=false
DOC_SUPPORTED_FORMATS=pdf,doc,docx,xlsx,xls,rtf,txt

# ===== ОЧЕРЕДИ И ФОНОВЫЕ ЗАДАЧИ =====

# Asynq (Redis-based задачи)
QUEUE_REDIS_ADDR=localhost:6379
QUEUE_CONCURRENCY=10
QUEUE_RETRY_DELAY=1m
QUEUE_MAX_RETRIES=5

# Количество воркеров для разных типов задач
WORKERS_SCRAPING=3
WORKERS_DOCUMENT_PROCESSING=5
WORKERS_AI_ANALYSIS=2
WORKERS_EMAIL=2
WORKERS_PRICE_ANALYSIS=1

# ===== МОНИТОРИНГ И ЛОГИРОВАНИЕ =====

# Prometheus метрики
MONITORING_ENABLED=true
MONITORING_METRICS_PORT=9090
MONITORING_METRICS_PATH=/metrics

# Уведомления
MONITORING_SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
MONITORING_EMAIL_ALERTS=true

# Логирование
LOG_LEVEL=info  # debug, info, warn, error
LOG_FORMAT=json  # json, text

# ===== ПОИСК И ИНДЕКСАЦИЯ =====

# Выбор поискового движка: elasticsearch, bleve
SEARCH_PROVIDER=bleve

# Elasticsearch (если используется)
ELASTICSEARCH_URLS=http://localhost:9200
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_INDEX=tender_data

# Bleve (встроенный поиск)
BLEVE_INDEX_PATH=./data/search_index

# ===== ФАЙЛОВОЕ ХРАНИЛИЩЕ =====

# Выбор провайдера: local, s3, minio
STORAGE_PROVIDER=local

# Локальное хранилище
STORAGE_LOCAL_BASE_PATH=./data/files

# AWS S3 (если используется)
STORAGE_S3_REGION=us-east-1
STORAGE_S3_BUCKET=tender-documents
STORAGE_S3_ACCESS_KEY=your-aws-access-key
STORAGE_S3_SECRET_KEY=your-aws-secret-key

# MinIO (если используется)
STORAGE_MINIO_ENDPOINT=localhost:9000
STORAGE_MINIO_ACCESS_KEY=minioadmin
STORAGE_MINIO_SECRET_KEY=minioadmin
STORAGE_MINIO_BUCKET=tender-documents
STORAGE_MINIO_USE_SSL=false

# ===== ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ =====

# Настройки безопасности
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Настройки производительности
HTTP_READ_TIMEOUT=30s
HTTP_WRITE_TIMEOUT=30s
HTTP_IDLE_TIMEOUT=60s

# Настройки кеширования
CACHE_TTL=1h
CACHE_CLEANUP_INTERVAL=10m

# ===== РАЗВЕРТЫВАНИЕ =====

# Docker настройки
DOCKER_REGISTRY=your-registry.com
DOCKER_IMAGE_TAG=latest

# Kubernetes настройки
K8S_NAMESPACE=tender-automation
K8S_REPLICAS=3

# ===== ИНСТРУКЦИИ ПО НАСТРОЙКЕ =====

# 🔧 ПОШАГОВАЯ НАСТРОЙКА:
#
# 1. БАЗЫ ДАННЫХ:
#    - Установите PostgreSQL и создайте БД tender_automation
#    - Установите Redis для очередей
#    - Опционально: MongoDB для документов
#
# 2. ИСКУССТВЕННЫЙ ИНТЕЛЛЕКТ:
#    - Установите Ollama: curl -fsSL https://ollama.ai/install.sh | sh
#    - Скачайте модель: ollama pull llama4-maviric:latest
#    - Или настройте OpenAI API ключ
#
# 3. EMAIL:
#    - Настройте Gmail App Password или корпоративный SMTP
#    - Разрешите доступ для менее безопасных приложений (если нужно)
#
# 4. ФАЙЛОВОЕ ХРАНИЛИЩЕ:
#    - Создайте директории для локального хранения
#    - Или настройте AWS S3/MinIO
#
# 5. МОНИТОРИНГ:
#    - Настройте Slack webhook для уведомлений
#    - Опционально: Prometheus для метрик
#
# 6. БЕЗОПАСНОСТЬ:
#    - Смените все пароли и секретные ключи
#    - Настройте firewall для production
#    - Используйте HTTPS в production

# ===== ПРИМЕР КОМАНД ЗАПУСКА =====

# Запуск локально:
# go run cmd/main.go

# Запуск с Docker:
# docker-compose up -d

# Запуск отдельных компонентов:
# go run cmd/main.go scraper     # Только парсер
# go run cmd/main.go processor   # Только обработка документов
# go run cmd/main.go emailer     # Только email автоматизация

# CLI команды:
# go run cmd/cli/main.go discover --platform=zakupki
# go run cmd/cli/main.go analyze --tender-id=12345
# go run cmd/cli/main.go stats --period=last-month